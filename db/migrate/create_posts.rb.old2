class CreatePosts < ActiveRecord::Migration
  def change
    create_table :posts do |t|
      t.string :title, :limit => 255 # default limit is 255, it's there for clarity
      t.text :body
      t.string :author, :limit => 127 # limited to 127 characters by user request
      t.date :pub_date
      t.string :status
      t.boolean :published

      t.timestamps
    end
  end
end
